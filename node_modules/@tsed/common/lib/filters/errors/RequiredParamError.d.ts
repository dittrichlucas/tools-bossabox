/**
 * @module common/mvc
 */
/** */
import { BadRequest } from "ts-httpexceptions";
import { IResponseError } from "../../mvc/interfaces/IResponseError";
/**
 * @private
 */
export declare class RequiredParamError extends BadRequest implements IResponseError {
    errors: any[];
    constructor(name: string, expression: string | RegExp);
    /**
     *
     * @param name
     * @param expression
     * @returns {string}
     */
    static buildMessage(name: string, expression: string): string;
}
