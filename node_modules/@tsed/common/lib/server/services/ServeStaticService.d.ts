import { ServerSettingsService } from "@tsed/common";
import * as Express from "express";
export declare class ServeStaticService {
    private expressApp;
    private serverSettingsService;
    constructor(expressApp: Express.Application, serverSettingsService: ServerSettingsService);
    $afterRoutesInit(): void;
    mount(path: string, directory: string): void;
}
