import { BadRequest } from "ts-httpexceptions";
import { IResponseError } from "../interfaces/IResponseError";
/**
 * @private
 */
export declare class ParseExpressionError extends BadRequest implements IResponseError {
    dataPath: string;
    requestType: string;
    errorMessage: string;
    origin: Error;
    constructor(name: string, expression: string | RegExp | undefined, err?: any);
    /**
     *
     * @param name
     * @param expression
     * @param message
     * @returns {string}
     */
    static buildMessage(name: string, expression: string | RegExp | undefined, message?: string): string;
}
