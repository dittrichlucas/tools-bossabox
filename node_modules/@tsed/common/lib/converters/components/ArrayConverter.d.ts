import { IConverter, IDeserializer, ISerializer } from "../interfaces/index";
/**
 * Converter component for the `Array` Type.
 * @private
 * @converters
 * @component
 */
export declare class ArrayConverter implements IConverter {
    /**
     *
     * @param data
     * @param target
     * @param baseType
     * @param deserializer
     * @returns {any[]}
     */
    deserialize<T>(data: any, target: any, baseType: T, deserializer: IDeserializer): T[];
    /**
     *
     * @param data
     * @param serializer
     * @returns {any[]}
     */
    serialize(data: any[], serializer: ISerializer): any[];
}
